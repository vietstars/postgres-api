#DB_HOST=docker.for.mac.localhost                             # when running the app without docker 

# Postgres Live
DB_HOST=postgres
DB_USER=benpro
DB_PASSWORD=master
DB_NAME=dev_master
DB_PORT=5432 

PGADMIN_DEFAULT_EMAIL=root@admin.com
PGADMIN_DEFAULT_PASSWORD=password

JWT_SECRET=
JWT_EXPIRED_IN=60m
